{% extends 'base.html.twig' %}

{% block javascripts %}
    {{ parent() }}
    <script src="https://cdn.jsdelivr.net/npm/typed.js@2.0.11"></script>
    <script>
        $(document).ready(function() {
            new Typed('#typed', {
                strings: [
                    'Nous répondons sous <b>48h</b>',
                    'On vous promet de <u>répondre vite</u> :)',
                    'Contactez-nous et vous verrez 😜'
                ],
                stringsElement: null,
                typeSpeed: 50,
                loop: true,
                showCursor: false,
            });
        })
    </script>
{% endblock %}

{% block title %}
    Contact pro
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-8 mx-auto">

            <div class="text-center">
                <h1>Contact PRO</h1>
                <div id="typed" style="height: 30px;"></div>
            </div>

            {{ form_start(form) }}

            <div class="row mt-3">
                <div class="col-md-6">
                    {{ form_row(form.firstname) }}
                </div>
                <div class="col-md-6">
                    {{ form_row(form.lastname) }}
                </div>
            </div>

            <div class="row mt-3">
                <div class="col-md-6">
                    {{ form_row(form.company) }}
                </div>
                <div class="col-md-6">
                    {{ form_row(form.email) }}
                </div>
            </div>

            <div class="mt-3">
                {{ form_row(form.subject) }}
            </div>

            <div class="mt-3">
                {{ form_row(form.message) }}
            </div>

            <div class="mt-3">
                {{ form_row(form.submit) }}
            </div>

            {{ form_end(form) }}

        </div>
    </div>
{% endblock %}
